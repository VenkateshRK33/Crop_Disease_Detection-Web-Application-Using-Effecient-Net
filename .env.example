# ============================================
# Plant Disease Detection Application
# Environment Configuration Template
# ============================================

# --------------------------------------------
# Backend Server Configuration
# --------------------------------------------

# Port for the Node.js Express backend server
# Default: 4000
# The React frontend is configured to proxy requests to this port
PORT=4000

# --------------------------------------------
# MongoDB Database Configuration
# --------------------------------------------

# MongoDB connection string
# For local development: mongodb://localhost:27017/plant-disease-db
# For MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# Default: mongodb://localhost:27017/plant-disease-db
MONGODB_URI=mongodb://localhost:27017/plant-disease-db

# --------------------------------------------
# Ollama LLM Configuration
# --------------------------------------------

# URL for the Ollama service (local LLM inference)
# Default: http://localhost:11434
# Ollama must be installed and running locally
OLLAMA_URL=http://localhost:11434

# Ollama model to use for chat responses
# Options: minimax-m2:cloud, llama2, mistral, etc.
# Default: minimax-m2:cloud
# Note: Model must be downloaded first using: ollama pull <model-name>
OLLAMA_MODEL=minimax-m2:cloud

# --------------------------------------------
# ML Service Configuration
# --------------------------------------------

# URL for the Python FastAPI ML service
# Default: http://localhost:5000
# This service handles plant disease prediction using EfficientNet
ML_API_URL=http://localhost:5000

# --------------------------------------------
# Optional Configuration
# --------------------------------------------

# Node environment (development, production, test)
# Default: development
NODE_ENV=development

# Enable debug logging (true/false)
# Default: false
DEBUG=false

# Maximum file upload size in MB
# Default: 10
MAX_FILE_SIZE_MB=10

# Session secret for authentication (if implementing auth)
# Generate a random string for production
# SESSION_SECRET=your-secret-key-here

# CORS allowed origins (comma-separated for multiple origins)
# Default: http://localhost:3000
# CORS_ORIGIN=http://localhost:3000,http://localhost:3001
